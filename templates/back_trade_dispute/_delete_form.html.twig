<form method="post" action="{{ path('app_admin_trade_dispute_delete', {'id': trade_dispute.id}) }}" onsubmit="return confirm('Are you sure you want to delete this trade dispute?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trade_dispute.id) }}">
    <button class="btn btn-danger btn-sm"><i class="bi bi-trash"></i></button>
</form>
