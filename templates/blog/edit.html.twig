{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">Edit Post</h1>
    
    <div class="card shadow-sm p-4">
        {{ include('blog/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="text-center mt-4">
        <a href="{{ path('app_blog_index') }}" class="btn btn-secondary">← Back to List</a>
        {{ include('blog/_delete_form.html.twig', { 'class': 'd-inline' }) }}
    </div>
</div>
{% endblock %}
